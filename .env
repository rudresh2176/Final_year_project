# Database URL for Prisma
# LOCAL DEV: uses SQLite file (set DATABASE_URL)
# PRODUCTION: set DATABASE_URL in the deployment platform's env vars panel
# If no DATABASE_URL is set, the app works with graceful degradation (no data history)
DATABASE_URL=file:db/custom.db
